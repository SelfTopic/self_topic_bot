import bot from "./bot";

bot.start();

// Add .env.example file. Env variables are not clear at the beginning of work.
// Bad error handling on the whole project. The issue is if you don't handle an error, node process is going down.
// The code lacks a consistent format.
// Duplicated code